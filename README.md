# Placeholder — Static site

This repository now contains a simple static webpage you can deploy via GitHub Pages.

Files added:

- `index.html` — main page
- `css/styles.css` — styles
- `images/` — placeholder logo and hero SVGs
- `404.html` — optional 404 page

Additional pages created:

- `about.html` — `O nas`
- `offer.html` — `Oferta`
- `gallery/photos.html` — `Galeria (Zdjęcia)`
- `gallery/videos.html` — `Galeria (Filmy)`
- `contact.html` — `Kontakt`

To preview locally, open `index.html` in a browser or use a lightweight dev server, for example with Python 3:

```bash
python -m http.server 8000
# then open http://localhost:8000
```

To publish with GitHub Pages:

1. Push this repository to GitHub.
2. In the repository settings, enable GitHub Pages from the `main` (or `master`) branch and root folder.
3. Optionally add a `CNAME` file with your custom domain.

Edit the content as needed — replace SVGs with real images and update text. Update navigation links if you change folder names.

Website link: https://wojciechimiolczyk.github.io/WebsiteSimple/